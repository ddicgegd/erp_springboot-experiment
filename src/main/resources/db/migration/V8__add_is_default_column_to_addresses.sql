-- V8: Add IS_DEFAULT column to ADDRESSES table
-- Oracle DB DDL Script

ALTER TABLE ADDRESSES ADD (IS_DEFAULT NUMBER(1) DEFAULT 0);

-- Populate existing records if any
UPDATE ADDRESSES SET IS_DEFAULT = 0 WHERE IS_DEFAULT IS NULL;
COMMIT;
