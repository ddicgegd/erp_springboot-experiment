-- V7: Add SKU column and unique constraint to ADDRESSES table
-- Oracle DB DDL Script

ALTER TABLE ADDRESSES ADD (SKU VARCHAR2(100));

-- Populate existing records if any
UPDATE ADDRESSES SET SKU = 'ADDR-' || TO_CHAR(ID) WHERE SKU IS NULL;
COMMIT;

ALTER TABLE ADDRESSES MODIFY (SKU NOT NULL);
ALTER TABLE ADDRESSES ADD CONSTRAINT UK_ADDRESSES_SKU UNIQUE (SKU);
